#test program for counting upto 50

#int i = 0;
sub r0 r0 r0            #zero r0
sti 0 r0                #mem[0] := r0(i)

#int max = 50;
sub r1 r1 r1            #zero r1
addi r1 r1 50           #r1(max) := r1(0) + 50
sti 1 r1                #mem[1] := r1(max=50)

#while(i < max)
#    i = i + 1;
ldi r0 0                #r0 := mem[0] = i
ldi r1 1                #r1 := mem[1] = max(50)
rb 2                    #jump to end of loop condition
addi r0 r0 1            #r0(i) := r0(i) + 1
rblt -1 r0 r1           #if r0(i) < r1(50) then jump back

#write back
sti 0 r0                #mem[0] := r1(i)