#include "enactor.h"

namespace Kraken
{

Scripture Enactor::enact(const Action &action,
                         const State *const state)
{
    err("Unimplemented: enact()\n");

    return Scripture();
}

};